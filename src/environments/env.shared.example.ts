// Copy this file to `env.shared.ts` and fill in your real API key.
export const sharedEnv = {
  geminiApiKey: 'REPLACE_WITH_YOUR_GEMINI_API_KEY'
};

// IMPORTANT: Do NOT commit `env.shared.ts` containing real keys. Add it to .gitignore.
